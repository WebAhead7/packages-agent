import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  addButton: {
    background: "#1EAC46",
    fontSize: 60,
    fontWeight: "bold",
  },
  addIcon: {
    color: "#fff",
    fontSize: 40,
  },
});

export default useStyles;
